from foo_param import calculate_sphere_volume

def main():
    radius = 5
    volume = calculate_sphere_volume(radius)
    print(f"The volume of the sphere with radius {radius} is {volume}")

if __name__ == "__main__":
    main()
