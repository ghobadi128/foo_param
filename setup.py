from setuptools import setup, find_packages

setup(
    name='foo_param',
    version='0.1.0',
    description='A package for calculating the Foo et al. parameterization of a sphere volume.',
    packages=find_packages(),
    install_requires=[],
    test_suite='tests',
)
